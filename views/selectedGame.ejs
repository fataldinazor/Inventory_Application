<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../styles/output.css" />
    <title>Game Details</title>
  </head>
  <body class="bg-gray-100 text-gray-900">
    <%- include("header.ejs") %>
    <div class="container mx-auto p-6 lg:p-8 bg-white shadow-lg rounded-lg flex flex-col lg:flex-row items-start">
      <!-- Left side: Image -->
      <img
        class="w-full lg:w-1/3 h-64 lg:h-auto object-cover rounded-lg lg:rounded-l-lg lg:rounded-none mb-4 lg:mb-0"
        src="<%=game.game_image%>"
        alt="<%=game.name%>"
      />

      <!-- Right side: Game info -->
      <div class="lg:w-2/3 lg:pl-8 flex flex-col">
        <h1 class="text-3xl lg:text-4xl font-bold mb-4"><%= game.name %></h1>
        <p class="text-gray-600 mb-2 font-medium">
          Developer: <%=game.developer%>
        </p>
        <p class="text-gray-600 mb-4">Released: <%= game.year %></p>

        <!-- Genres as capsules -->
        <ul class="flex flex-wrap gap-2 mb-4">
          <% genres.forEach(genre => { %>
          <li class="bg-gray-200 text-gray-700 px-3 py-1 rounded-full text-sm">
            <%=genre.genre%>
          </li>
          <% }) %>
        </ul>

        <p class="text-gray-700 leading-relaxed mb-6">
          <%= game.description %>
        </p>

        <h3 class="text-xl lg:text-2xl font-semibold mb-6">Rating: <%=game.rating%></h3>

        <div class="flex flex-col sm:flex-row gap-4">
          <a
            href="/games/<%=game.game_id%>/edit"
            class="bg-blue-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-blue-600 transition duration-300 text-center"
          >
            Update
          </a>
          <form action="/games/<%=game.game_id%>/delete" method="POST" class="flex flex-col sm:flex-row gap-4">
            <label for="password" class="flex items-center gap-2">
              Password:
              <input
                type="password"
                id="password"
                name="password"
                required
                class="border border-gray-300 rounded-lg py-2 px-4 focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
            </label>
            <button
              onclick="return confirm('Are you sure you want to delete this item?')"
              type="submit"
              class="bg-red-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-red-600 transition duration-300"
            >
              Delete
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
